1. instalar o node.js (nodejs.org)
2. npm --version (check do nodejs instalado)
3. instalar o angular (https://angular.io/guide/setup-local)
    3.1 npm install @angular/cli
Executar os comandos  (npm install -g @angular/cli) (Set-ExecutionPolicy -Scope CurrentUser -ExecutionPolicy RemoteSigned)
4. instalar o angular


GIT IGNORE (EVITA ALGUMAS COISAS A IR PRO GITHUB)

# ROTINAS DO GIT
(git clone link)
1. sempre executar o git status (verificar situação)
2. se vermelho -> git add.
3. se verde git commit -m "nome do commit"
4.se tudo ok, git push (atualiza no git hub)

5. git clone endereço.git . (comando para copiar do githun para o vscode)
5.1 npm install (dentro da pasta do projeto para instalar o nmodules)


-------- para remover usuario anterior ---------

git config --system --unset credential.helper

npm install -> instala o que está em falta

#---- angular

ng new nome-projeto (criar um projeto)

TRABALHO FRAMEWORKS

https://www.figma.com/ OU CANVA

ng serve -o (rodar e abrir o projeto)




